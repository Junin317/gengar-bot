[phases.setup]
nixPkgs = ["chromium"]

[phases.build]
cmds = ["which chromium || which chromium-browser || find / -name 'chromium' 2>/dev/null | head -5"]

[variables]
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD = "true"